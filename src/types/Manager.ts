export interface Manager {
    _id: string;
    name: string;
    email: string;
    phoneNumber: string;
  }

  export interface ManagerFormData {
    name: string;
    email: string;
    phoneNumber: string;
  }

